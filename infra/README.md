# Infrastructure

Infrastructure-as-code and deployment assets (Docker images, compose overrides,
Terraform/CloudFormation, AWS/ECS definitions) will live here. Empty at scaffold
stage — local Postgres/Redis are provided by the root docker-compose.yml.
