import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Scaffold: image/remote-pattern and rewrites (e.g. to the API) will be added later.
};

export default nextConfig;
